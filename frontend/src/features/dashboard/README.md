# features/dashboard/

Dashboard UI state persistence — view mode and active admin tab.

Stored in `localStorage` under `nutkeep_dashboard` key.
Exports: `DashboardProvider`, `useDashboard`, `FilesViewMode`, `AdminTab`.
